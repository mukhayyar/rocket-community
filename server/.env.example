# Server Configuration
PORT=3000

# Optional: Database path (defaults to ./rockets.db)
# DATABASE_URL=/data/rockets.db

# Optional: CORS origins (defaults to all origins)
# CORS_ORIGIN=https://example.com
